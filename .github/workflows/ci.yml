name: Vocalinux CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: [3.8, 3.9, '3.10', '3.11']

    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}
        
    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          xdotool \
          python3-gi \
          gir1.2-appindicator3-0.1 \
          libcairo2-dev \
          pkg-config \
          python3-dev \
          libgirepository1.0-dev \
          gobject-introspection \
          libcairo-gobject2 \
          gir1.2-gtk-3.0
        
    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        # Skip the direct PyGObject installation and use system packages
        python -m pip install -e .
        python -m pip install pytest pytest-cov black isort flake8
        
    - name: Lint with flake8
      run: |
        # stop the build if there are Python syntax errors or undefined names
        flake8 src/ tests/ --count --select=E9,F63,F7,F82 --show-source --statistics
        # exit-zero treats all errors as warnings
        flake8 src/ tests/ --count --exit-zero --max-complexity=10 --max-line-length=100 --statistics
        
    - name: Check formatting with black
      run: |
        black --check src/ tests/
        
    - name: Check import sorting with isort
      run: |
        isort --check-only --profile black src/ tests/
        
    - name: Run basic tests
      run: |
        # Make sure Python can find the 'src' module
        PYTHONPATH=$PWD python tests/run_basic_tests.py
        
    - name: Run pytest suite with coverage
      run: |
        # Make sure Python can find the 'src' module
        PYTHONPATH=$PWD pytest --cov=src tests/
        
    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v3
      with:
        fail_ci_if_error: false