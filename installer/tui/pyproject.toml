[project]
name = "vocalinux-installer-tui"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["textual>=8.0,<9.0"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
